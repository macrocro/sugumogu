<!DOCTYPE html>
<html lang="ja">
  <head>
 <%
    if controller.action_name == 'pref'
      title = @ja_pref + 'のダイビングショップ'
      canonical = shop_pref_url(@pref)
    elsif controller.action_name == 'city'
      title = @ja_pref + @ja_city + 'のダイビングショップ'
      canonical = shop_city_url(@pref,@city)
    end
 %>
 <%= display_meta_tags({
         :title => title,
         :site => @meta[:site],
         :description => @meta[:description],
         :keywords => @meta[:keywords],
         :open_graph => {
            :title => @meta[:title],
            :type  => 'website',
            :image => @meta[:image],
            :url   => @meta[:canonical],
            :description => @meta[:description],
            :locale => 'ja_JP'
         },
        :fb => {
            :app_id => @meta[:fb_app_id],
            :page_id => @meta[:fb_page_id],
        },
        :twitter => {
            :card => 'product',
            :site => '@sugumogu',
            :url => @meta[:canonical],
            :title => @meta[:title],
            :description => @meta[:description],
            :image => @meta[:image]
         },
        :canonical => canonical,
        :separator => ' - ',
        :reverse => true
     })
 %>
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">

    <%= stylesheet_link_tag    "bootstrap", media: "all", "data-turbolinks-track" => true %>
    <%= stylesheet_link_tag    "main.css", media: "all", "data-turbolinks-track" => true %>

    <script src="//ajax.googleapis.com/ajax/libs/jquery/1.10.2/jquery.min.js"></script>
    <script src="//maps.google.com/maps/api/js?v=3&sensor=false" type="text/javascript" charset="UTF-8"></script>
    <%= javascript_include_tag "shop", "data-turbolinks-track" => true %>

    <%= csrf_meta_tags %>
  </head>
  <body>
    <div class="navbar navbar-fixed-top" id="page-header">
      <div class="container container-header">
        <img src="http://www.khb-tv.co.jp/image/logo_main.gif" class="title-logo">
      </div>
    </div>
    <div class="container">
      <%= render_breadcrumbs %>
      <div class="main">
        <div class="shop-container">
          <%= yield %>
        </div>
      </div>
    </div>
  </body>
</html>
